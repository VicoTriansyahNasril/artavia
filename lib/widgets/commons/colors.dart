part of 'common.dart';

// ─── Base Palette ─────────────────────────────────────────────────────────────
const Color colorWhite = Color(0xFFFFFFFF);
const Color colorBlack = Color(0xFF000000);
const Color colorGrey = Color(0xFF8B93A5); // Lighter blue-grey for Astro theme

// ─── Background Layers ────────────────────────────────────────────────────────
/// Deepest background — Astro Space Void
const Color colorBackground = Color(0xFF0A0B10);
/// Elevated surface — Spaceship interior / hardware plastics
const Color colorCard = Color(0xFF161824);
/// Slightly lighter card for nested elements
const Color colorSurface = Color(0xFF1E2130);
/// Subtle divider / hairline separator
const Color colorDivider = Color(0xFF2A2D40);

// ─── Brand Accent (Astro Neon Cyan) ──────────────────────────────────────────
/// Primary accent — Astro Bot Neon Cyan (Vibrant and futuristic)
const Color colorAccent = Color(0xFF00D8FF);
/// Text/Icon color to use on top of colorAccent
const Color colorOnAccent = Color(0xFF000000); // Black text on neon cyan pops well
/// Dimmed accent for backgrounds / overlays
const Color colorAccentDim = Color(0xFF00384D);

// ─── Semantic Colors (Neon / Playful) ─────────────────────────────────────────
/// Pengeluaran — Hot Neon Pink/Red
const Color colorExpense = Color(0xFFFF2A6D);
/// Pemasukan — Vibrant Neon Green
const Color colorIncome = Color(0xFF05FF97);
/// Info / neutral highlight
const Color colorInfo = Color(0xFF00D8FF);

// ─── Deprecated (kept for compatibility) ─────────────────────────────────────
const Color colorRed = Color(0xFFFF2A6D);
const Color colorBorder = Color(0xFF2A2D40);
const Color colorDisabledFill = Color(0xFF2A2D40);

