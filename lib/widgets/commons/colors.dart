part of 'common.dart';

// ─── Base Palette ─────────────────────────────────────────────────────────────
const Color colorWhite = Color(0xFFFFFFFF);
const Color colorBlack = Color(0xFF000000);
const Color colorGrey = Color(0xFF9095A0); // Cool silver-grey for less contrast text

// ─── Background Layers ────────────────────────────────────────────────────────
/// Deepest background — sleek dark cool grey (BMW dashboard feel)
const Color colorBackground = Color(0xFF121418);
/// Elevated surface — cards, list containers
const Color colorCard = Color(0xFF1B1E24);
/// Slightly lighter card for nested elements
const Color colorSurface = Color(0xFF24272E);
/// Subtle divider / hairline separator
const Color colorDivider = Color(0xFF2E323A);

// ─── Brand Accent (BMW Classic Blue) ──────────────────────────────────────────
/// Primary accent — Classic Bavarian Blue (Deep, vibrant but not neon)
const Color colorAccent = Color(0xFF005EAA);
/// Text/Icon color to use on top of colorAccent
const Color colorOnAccent = Color(0xFFFFFFFF);
/// Dimmed accent for backgrounds / overlays
const Color colorAccentDim = Color(0xFF002244);

// ─── Semantic Colors ──────────────────────────────────────────────────────────
/// Pengeluaran — BMW M Red inspired (mature, strong)
const Color colorExpense = Color(0xFFD32F2F);
/// Pemasukan — calm mature green
const Color colorIncome = Color(0xFF388E3C);
/// Info / neutral highlight
const Color colorInfo = Color(0xFF1976D2);

// ─── Deprecated (kept for compatibility) ─────────────────────────────────────
const Color colorRed = Color(0xFFD32F2F);
const Color colorBorder = Color(0xFF2E323A);
const Color colorDisabledFill = Color(0xFF2E323A);

