part of 'common.dart';

// ─── Base Palette ─────────────────────────────────────────────────────────────
const Color colorWhite = Color(0xFFFFFFFF);
const Color colorBlack = Color(0xFF000000);
const Color colorGrey = Color(0xFF8A8A8E); // iOS-style system gray

// ─── Background Layers ────────────────────────────────────────────────────────
/// Deepest background — scaffold bg
const Color colorBackground = Color(0xFF111214);
/// Elevated surface — cards, list containers
const Color colorCard = Color(0xFF1C1C1F);
/// Slightly lighter card for nested elements
const Color colorSurface = Color(0xFF242428);
/// Subtle divider / hairline separator
const Color colorDivider = Color(0xFF2A2A2E);

// ─── Brand Accent ─────────────────────────────────────────────────────────────
/// Primary accent — warm amber (not neon yellow)
const Color colorAccent = Color(0xFFF5C842);
/// Dimmed accent for backgrounds / overlays
const Color colorAccentDim = Color(0xFF3D3012);

// ─── Semantic Colors ──────────────────────────────────────────────────────────
/// Pengeluaran — muted rose (not harsh red)
const Color colorExpense = Color(0xFFE57373);
/// Pemasukan — calm sage green
const Color colorIncome = Color(0xFF81C784);
/// Info / neutral highlight
const Color colorInfo = Color(0xFF64B5F6);

// ─── Deprecated (kept for compatibility) ─────────────────────────────────────
const Color colorRed = Color(0xFFE57373);
const Color colorBorder = Color(0xFF2A2A2E);
const Color colorDisabledFill = Color(0xFF2A2A2E);
